<Page x:ConnectionId='1'
    x:Class="QuarterbackRating.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuarterbackRating"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:QuarterbackRating.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>

        <TextBlock x:ConnectionId='2' Grid.Row="0" 
                   Name="Header"
                   FontSize="24"
                   Foreground="Red"
                   FontWeight="Bold"
                   Text="QuarterBack Rating"/>
        
        <GridView x:ConnectionId='3' Grid.Row="1"                                     IsItemClickEnabled="True"                               >
            <GridView.ItemTemplate>
                <DataTemplate                              >
                    <StackPanel x:ConnectionId='6' Orientation="Horizontal" HorizontalAlignment="Left">
                        <StackPanel Margin="20,20,0,0">
                            <TextBlock x:ConnectionId='7' FontSize="16"                      HorizontalAlignment="Left"/>
                            <TextBlock x:ConnectionId='8' FontSize="10"                        HorizontalAlignment="left"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        
        <TextBlock x:ConnectionId='4' Grid.Row="2" 
                   Name="ResultTextBlock"
                   FontSize="24"
                   Foreground="Red"
                   FontWeight="Bold"/>
    </Grid>
</Page>

